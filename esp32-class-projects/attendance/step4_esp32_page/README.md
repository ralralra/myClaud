# 4단계 — ESP32가 웹페이지를 보여주기

## 목표
폰으로 ESP32에 접속하면 출석 페이지가 뜬다. **드디어 ESP32 등장!**

## 핵심 아이디어 (가장 중요)
지금까진 PC에서 `.html` 파일을 직접 열었죠.
이제는 **그 HTML을 ESP32 코드 안에 통째로 넣어두고**, 폰이 접속하면 ESP32가 그걸 보내줍니다.

```
PC에서:   .html 파일 더블클릭  →  브라우저가 화면 표시
ESP32에서: 폰이 192.168.4.1 접속  →  ESP32가 HTML 보냄  →  폰 브라우저가 화면 표시
```

**같은 HTML, 보내주는 주체만 ESP32로 바뀐 것!**

## 코드에서 봐야 할 곳

```cpp
const char* page = R"HTML(
  ... 여기에 1단계 HTML이 통째로 들어있음 ...
)HTML";
```
- `R"HTML( ... )HTML"` = 긴 HTML을 따옴표 걱정 없이 그대로 담는 방법

```cpp
void handleRoot() {
  server.send(200, "text/html; charset=utf-8", page);  // 접속하면 page 보냄
}
```

```cpp
WiFi.softAP(AP_SSID, AP_PASS);   // ESP32가 스스로 와이파이를 만든다
```

### 맨 위의 낯선 두 줄 + setup 첫 줄은 뭐예요?
```cpp
#include "soc/soc.h"
#include "soc/rtc_cntl_reg.h"
...
WRITE_PERI_REG(RTC_CNTL_BROWN_OUT_REG, 0);  // setup() 첫 줄
```
ESP32가 와이파이를 켤 때 전류가 확 필요해서 전압이 잠깐 출렁이는데, 이때 보드가
"전원 부족!"이라며 스스로 꺼지는(Brownout) 걸 막아주는 **안전장치**예요.
없어도 대부분 동작하지만, 넣어두면 더 안정적입니다. (지금은 그냥 그대로 두면 됨)

## 해보기
1. `step4_esp32_page.ino` 열기 → 보드 **ESP32 Dev Module** 선택 → **Upload**
2. 업로드 끝나면 시리얼 모니터(115200) 열기
3. 이런 메시지 확인:
   ```
   와이파이 이름: ESP32-Attend
   접속 주소:    http://192.168.4.1
   서버 시작됨!
   ```
4. **폰** 와이파이 설정 → `ESP32-Attend` 선택 → 비번 `attend1234`
5. 폰이 "인터넷 없음, 계속 연결?" 하면 **계속 연결**
6. 폰 브라우저(크롬 등) 주소창에 **192.168.4.1** 입력
7. 출석 페이지가 뜨면 성공! 🎉

## 확인 ✅
- [ ] 시리얼에 접속 주소(192.168.4.1)가 떴다
- [ ] 폰 와이파이 목록에 ESP32-Attend가 보인다
- [ ] 폰으로 접속하니 출석 페이지가 떴다
- [ ] 맨 아래 "이 페이지는 ESP32가 보냈습니다" 가 보인다
- [ ] 버튼 누르면 안내 팝업이 뜬다 (아직 진짜 출석은 X — 5단계에서)

## 잘 안 될 때
- **`Brownout detector was triggered` 가 반복되고 "서버 시작됨"이 안 뜸** → 업로드 후 보드가 스케치로 안 넘어간 것. **IO0(BOOT) + RESET(EN) 버튼을 동시에 눌렀다 떼서 강제 재부팅** → 보통 바로 실행됨 (실전에서 가장 흔한 막힘!)
- 페이지 안 뜸 → 주소는 **http://192.168.4.1** (https 아님!)
- 와이파이 안 보임 → 시리얼에 "서버 시작됨" 떴는지 먼저 확인. `ESP32-Attend` 를 집 와이파이와 같은 이름으로 두지 말 것
- 자세히는 [`../docs/troubleshooting.md`](../docs/troubleshooting.md)

## 직접 바꿔보기
- `AP_SSID` 를 `ESP32-3반` 처럼 우리 반으로 바꾸고 다시 업로드
- `page` 안의 제목 글자를 바꾸고 업로드 → 폰에서 새로고침

## 배운 것
- ESP32는 **웹서버** 가 될 수 있다 (페이지를 보내주는 역할)
- HTML을 코드 안 문자열로 넣는다
- AP 모드 = ESP32가 직접 와이파이를 만든다 (인터넷 불필요)

## 다음 단계
→ `step5_send_data` : 출석 버튼을 누르면 이름이 진짜로 ESP32에 전달되게
